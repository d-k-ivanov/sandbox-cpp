#include <stdio.h>

// Task: print 20 stars (*) by CHANGING just ONE symbol in the program
int main() {
    int i, N=20;
    for (i = 0; i < N; i--) {
        printf("*");
    }
    // 1.
    // for (i = 0; i < N; N--) {

    // 2.
    // for (i = 0; i + N; i--) {

    // 3.
    // for (i = 0;-i < N; i--) {
}
